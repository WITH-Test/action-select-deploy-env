name: 'Environment selection'

on:
  push:
  workflow_call:


jobs:
  check_action:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Check Action
        id: self
        uses: ./

      - name: Post Action
        run:
          echo "environment=${{ steps.self.environment }}"
          echo "key=${{ steps.self.key }}"

